schema_version: 1

factor_id: runtime

bindings:
  - id: runtime_authorization
    when:
      evidence_category: runtime_authorization
    steps:
      - ref: steps/runtime/invoke_api.step.manual.yaml
