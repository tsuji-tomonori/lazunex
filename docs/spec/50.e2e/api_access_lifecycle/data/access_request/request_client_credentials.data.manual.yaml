schema_version: 1

data:
  id: client_credentials_auth_mode
  title: requestedAuthMode=CLIENT_CREDENTIALS
  request:
    body:
      apiId: ${api.apiId}
      apiStageId: ${api.apiStageId}
      requestedAuthMode: CLIENT_CREDENTIALS
