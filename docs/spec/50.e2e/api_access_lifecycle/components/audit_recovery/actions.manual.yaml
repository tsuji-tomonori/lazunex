schema_version: 1

actions:
  - id: collect_audit
    title: 監査イベントを確認する
    operation_type: evidence
    default_result: recorded
    target:
      - project
      - api

  - id: retry_after_failure
    title: 失敗後に再試行する
    operation_type: command
    default_result: retryable
    target:
      - project
      - api
