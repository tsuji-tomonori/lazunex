schema_version: 1

step:
  id: management_list_projects
  title: Project一覧APIを呼び出す
  phase: evidence
  operation_id: listProjects
  endpoint: GET /projects

request:
  query:
    keyword: ${project.defaults.projectCode}

expect:
  response:
    status: 200
