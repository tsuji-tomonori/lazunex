schema_version: 1

step:
  id: management_get_api
  title: API詳細APIを呼び出す
  phase: evidence
  operation_id: getApi
  endpoint: GET /apis/${apiId}

request:
  path:
    apiId: ${api.id}.apiId

expect:
  response:
    status: 200
