schema_version: 1

step:
  id: update_project_public_client
  title: Project public client更新APIを呼び出す
  phase: exercise
  operation_id: updateProjectPublicClient
  endpoint: PATCH /projects/{projectId}/public-client

md:
  detail_section:
    include: true
    heading: Project public client更新APIを呼び出す
    description: ${project.id}のcallback/logout URLを更新し、既存scopeを保持する。

prerequisites:
  - id: project_created
    text: ${project.id} が作成済みである。

request:
  source: data.request

expect:
  response:
    status: ${result.expected_status}
