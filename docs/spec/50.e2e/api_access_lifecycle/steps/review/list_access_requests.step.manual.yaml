schema_version: 1

step:
  id: review_list_access_requests
  title: 利用申請一覧取得APIを呼び出す
  phase: verify
  operation_id: listProjectApiAccessRequests
  endpoint: GET /projects/{projectId}/api-access-requests

md:
  detail_section:
    include: false
    heading: 利用申請一覧取得APIを呼び出す
    description: レビュー後の利用申請状態を確認する。
