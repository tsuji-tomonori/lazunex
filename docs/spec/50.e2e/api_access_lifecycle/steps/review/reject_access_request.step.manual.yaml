schema_version: 1

step:
  id: reject_access_request
  title: 却下APIを呼び出す
  phase: exercise
  operation_id: rejectApiAccessRequest
  endpoint: POST /api-access-requests/{accessRequestId}/reject

md:
  detail_section:
    include: true
    heading: 却下APIを呼び出す
    description: ${project.id} × ${api.id} の利用申請を却下する。

prerequisites:
  - id: access_request_pending
    text: accessRequestId[${project.id},${api.id}] がPENDINGである。
  - id: reviewer_permission
    text: 呼び出し元が ${api.id} のreviewerである。

request:
  source: data.request

expect:
  response:
    status: ${result.expected_status}
