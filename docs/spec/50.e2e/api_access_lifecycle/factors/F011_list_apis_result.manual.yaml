# This file is maintained manually.
schema_version: 1

factor:
  id: F011
  slug: list_apis_result
  title: "GET /apis API一覧取得結果"
  category: api_result
  order: 11
  owner_step: get_apis
  generated_from:
    - type: e2e_flow
      path: docs/spec/50.e2e/api_access_lifecycle/flow.manual.yaml

elements:
  - id: success
    label: "成功: 一覧に公開APIが現れる"
    default: true
    terminal: false
    expected:
      summary: "HTTP 200、itemsに公開済みapiId/apiCode/scopeが含まれる、pagination/filterが仕様どおり、secret値を含まない。"
  - id: filter_no_match
    label: "失敗相当: filter不一致"
    default: false
    terminal: true
    expected:
      summary: "HTTP 200かつitems空配列を返し、後続getApiは既知apiIdを使って継続する。"

constraints:
  requires:
    - F010.success

pruning:
  after_terminal: true
  equivalent_if_same_terminal_step: true
