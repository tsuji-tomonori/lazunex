# This file is maintained manually.
schema_version: 1

factor:
  id: F023
  slug: update_project_public_client_result
  title: "PATCH /projects/{projectId}/public-client 更新結果"
  category: api_result
  order: 23
  owner_step: patch_project_public_client
  generated_from:
    - type: e2e_flow
      path: docs/spec/50.e2e/api_access_lifecycle/flow.manual.yaml

elements:
  - id: success
    label: "成功: public client設定更新 + 承認済みscope保持"
    default: true
    terminal: false
    expected:
      summary: "HTTP 200、callback/logout/token設定が更新され、既存AllowedOAuthScopesと承認済みscopeを消さない。"
  - id: invalid_redirect_uri
    label: "失敗: redirect URI不正"
    default: false
    terminal: true
    expected:
      summary: "HTTP 400/409を返し、既存public client設定と承認済みscopeを変更しない。"

constraints:
  requires:
    - F020.success
    - F022.success

pruning:
  after_terminal: true
  equivalent_if_same_terminal_step: true
