# This file is maintained manually.
schema_version: 1

factor:
  id: F002
  slug: management_auth
  title: "管理API認証"
  category: auth
  order: 2
  owner_step: management_api
  generated_from:
    - type: e2e_flow
      path: docs/spec/50.e2e/api_access_lifecycle/flow.manual.yaml

elements:
  - id: valid
    label: "有効なmanagement token"
    default: true
    terminal: false
    expected:
      summary: "管理API呼び出しが認証を通過する。"
  - id: missing
    label: "Authorizationなし"
    default: false
    terminal: true
    expected:
      summary: "最初の管理APIでHTTP 401となり後続stepを実行しない。"

constraints:
  requires: []

pruning:
  after_terminal: true
  equivalent_if_same_terminal_step: true
