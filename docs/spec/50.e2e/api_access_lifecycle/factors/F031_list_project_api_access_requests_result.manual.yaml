# This file is maintained manually.
schema_version: 1

factor:
  id: F031
  slug: list_project_api_access_requests_result
  title: "GET /projects/{projectId}/api-access-requests 利用申請一覧取得結果"
  category: api_result
  order: 31
  owner_step: get_project_api_access_requests
  generated_from:
    - type: e2e_flow
      path: docs/spec/50.e2e/api_access_lifecycle/flow.manual.yaml

elements:
  - id: success
    label: "成功: PENDING申請が一覧に現れる"
    default: true
    terminal: false
    expected:
      summary: "HTTP 200、itemsにaccessRequestId/apiId/requestedAuthMode/stateが含まれ、Project単位に絞られる。"
  - id: unauthorized_project
    label: "失敗: Project権限なし"
    default: false
    terminal: true
    expected:
      summary: "HTTP 403を返し、審査API以降を実行しない。"

constraints:
  requires:
    - F030.success

pruning:
  after_terminal: true
  equivalent_if_same_terminal_step: true
