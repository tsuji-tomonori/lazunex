schema_version: 1

factor:
  id: access_request
  title: API利用申請
  dimensions:
    project:
      source: targets/projects/*.target.manual.yaml
    api:
      source: targets/apis/*.target.manual.yaml
    operation:
      source: operations/access_request/*.operation.manual.yaml
    result:
      source: results/common/*.result.manual.yaml
    data:
      source: data/access_request/*.data.manual.yaml
