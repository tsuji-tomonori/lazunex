# This file is maintained manually.
schema_version: 1

factor:
  id: F070
  slug: runtime_credential
  title: "Runtime credentials"
  category: runtime_auth
  order: 70
  owner_step: invoke_runtime_api
  generated_from:
    - type: e2e_flow
      path: docs/spec/50.e2e/api_access_lifecycle/flow.manual.yaml

elements:
  - id: valid_token_valid_api_key
    label: "正常token + API key"
    default: true
    terminal: false
    expected:
      summary: "Runtime APIが期待する2xxを返す。"
  - id: scope_missing
    label: "scopeなしtoken"
    default: false
    terminal: false
    expected:
      summary: "Runtime APIが認可エラーを返す。"
  - id: api_key_missing
    label: "API keyなし"
    default: false
    terminal: false
    expected:
      summary: "Runtime APIが認可エラーを返す。"

constraints:
  requires:
    - F061.success

pruning:
  after_terminal: true
  equivalent_if_same_terminal_step: true
