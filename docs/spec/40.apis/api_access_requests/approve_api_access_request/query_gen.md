<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# approve_api_access_request query

## 001_select_api_access_requests.sql

### SQLの概要

承認対象の利用申請と現在状態を確認するため、利用申請を取得する。

### 利用するテーブル

- `api_access_requests`
- `projects`
- `apis`
- `api_gateway_stages`
- `api_cognito_scopes`
- `api_access_reviews`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_access_requests</code> | <code>access_request_id</code> | <code>UUID</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_access_requests</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>api_access_requests</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>api_access_requests</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_access_requests</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>api_access_requests</code> | <code>requested_auth_mode</code> | <code>str</code> | no |
| <code>api_access_requests</code> | <code>requested_reason</code> | <code>str</code> | no |
| <code>api_access_requests</code> | <code>requested_by</code> | <code>str</code> | no |
| <code>api_access_requests</code> | <code>requested_at</code> | <code>datetime</code> | no |
| <code>projects</code> | <code>project_code</code> | <code>str</code> | no |
| <code>projects</code> | <code>owner_principal_id</code> | <code>str</code> | no |
| <code>apis</code> | <code>api_code</code> | <code>str</code> | no |
| <code>apis</code> | <code>api_name</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>apigw_rest_api_id</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>apigw_stage_name</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>api_scope_id</code> | <code>UUID</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_full_name</code> | <code>str</code> | no |

### 条件

- `JOIN ON projects.project_id = api_access_requests.project_id`
- `JOIN ON apis.api_id = api_access_requests.api_id`
- `JOIN ON api_gateway_stages.api_stage_id = api_access_requests.api_stage_id`
- `JOIN ON api_cognito_scopes.api_id = api_access_requests.api_id`
- `WHERE api_access_requests.access_request_id = @access_request_id AND NOT EXISTS(SELECT 1 FROM api_access_reviews AS rv WHERE api_access_reviews.access_request_id = api_access_requests.access_request_id)`


## 002_select_api_reviewers.sql

### SQLの概要

承認者が対象APIのreviewerか確認するため、API reviewerを取得する。

### 利用するテーブル

- `api_reviewers`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_reviewers</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_reviewers</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>-</code> | <code>is_hub_admin</code> | <code>Any</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_reviewers</code> | <code>api_reviewer_id</code> | <code>UUID</code> | no |
| <code>api_reviewers</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_reviewers</code> | <code>reviewer_principal_id</code> | <code>str</code> | no |
| <code>api_reviewers</code> | <code>reviewer_role</code> | <code>str</code> | no |

### 条件

- `WHERE api_id = @api_id AND (reviewer_principal_id = @actor_principal_id OR @is_hub_admin = TRUE)`


## 003_select_subscriptions.sql

### SQLの概要

重複承認を防ぐため、既存のactive subscriptionを取得する。

### 利用するテーブル

- `project_api_subscriptions`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_api_subscriptions</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>api_stage_id</code> | <code>UUID</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_api_subscriptions</code> | <code>subscription_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>approved_auth_mode</code> | <code>str</code> | no |
| <code>project_api_subscriptions</code> | <code>approved_by</code> | <code>str</code> | no |
| <code>project_api_subscriptions</code> | <code>approved_at</code> | <code>datetime</code> | no |

### 条件

- `WHERE project_id = @project_id AND api_stage_id = @api_stage_id`


## 004_insert_access_request_events.sql

### SQLの概要

承認処理の開始と完了を追跡するため、利用申請イベントを追加する。

### 利用するテーブル

- `access_request_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>access_request_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>access_request_events</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>access_request_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>access_request_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>access_request_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 005_insert_provisioning_operations.sql

### SQLの概要

承認後のAWS反映作業を追跡するため、provisioning operationを追加する。

### 利用するテーブル

- `provisioning_operations`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operations</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operations</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_operations</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operations</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 006_select_project_cognito_clients.sql

### SQLの概要

承認後にscopeを付与する対象を決めるため、Project Cognito clientを取得する。

### 利用するテーブル

- `project_cognito_clients`
- `project_usage_plans`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_cognito_clients</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>-</code> | <code>approved_auth_mode</code> | <code>Any</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_cognito_clients</code> | <code>project_cognito_client_id</code> | <code>UUID</code> | no |
| <code>project_cognito_clients</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_cognito_clients</code> | <code>client_type</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>cognito_user_pool_id</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>app_client_id</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>base_allowed_scopes</code> | <code>dict[str, Any]</code> | no |
| <code>project_cognito_clients</code> | <code>allowed_oauth_flows</code> | <code>dict[str, Any]</code> | no |
| <code>project_usage_plans</code> | <code>project_usage_plan_id</code> | <code>UUID</code> | no |
| <code>project_usage_plans</code> | <code>apigw_usage_plan_id</code> | <code>str</code> | no |

### 条件

- `JOIN ON project_usage_plans.project_id = project_cognito_clients.project_id`
- `WHERE project_cognito_clients.project_id = @project_id AND ((@approved_auth_mode = 'PUBLIC_PKCE' AND project_cognito_clients.client_type = 'PUBLIC_PKCE') OR (@approved_auth_mode = 'CLIENT_CREDENTIALS' AND project_cognito_clients.client_type = 'CONFIDENTIAL_CLIENT_CREDENTIALS') OR (@approved_auth_mode = 'BOTH'))`


## 007_insert_api_access_reviews.sql

### SQLの概要

承認結果と承認コメントを保持するため、利用申請レビューを追加する。

### 利用するテーブル

- `api_access_reviews`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_access_reviews</code> | <code>access_review_id</code> | <code>UUID</code> | no |
| <code>api_access_reviews</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>api_access_reviews</code> | <code>approved_auth_mode</code> | <code>str</code> | no |
| <code>api_access_reviews</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_access_reviews</code> | <code>review_comment</code> | <code>str</code> | no |
| <code>api_access_reviews</code> | <code>now</code> | <code>datetime</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 008_insert_project_api_subscriptions.sql

### SQLの概要

承認済みAPI利用権を有効化するため、Project API subscriptionを追加する。

### 利用するテーブル

- `project_api_subscriptions`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_api_subscriptions</code> | <code>subscription_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>project_api_subscriptions</code> | <code>approved_auth_mode</code> | <code>str</code> | no |
| <code>project_api_subscriptions</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_api_subscriptions</code> | <code>now</code> | <code>datetime</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 009_insert_project_usage_plan_api_stages.sql

### SQLの概要

Usage Planから対象stageを利用可能にするため、Usage Plan stage紐づけを追加する。

### 利用するテーブル

- `project_usage_plan_api_stages`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_usage_plan_api_stages</code> | <code>usage_plan_api_stage_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>project_usage_plan_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>subscription_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>apigw_rest_api_id</code> | <code>str</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>apigw_stage_name</code> | <code>str</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_usage_plan_api_stages</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 010_insert_project_cognito_client_scopes.sql

### SQLの概要

Cognito clientにAPI実行scopeを許可するため、Project Cognito client scopeを追加する。

### 利用するテーブル

- `project_cognito_client_scopes`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_cognito_client_scopes</code> | <code>project_cognito_client_scope_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_scopes</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_scopes</code> | <code>project_cognito_client_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_scopes</code> | <code>api_scope_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_scopes</code> | <code>subscription_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_scopes</code> | <code>scope_full_name</code> | <code>str</code> | no |
| <code>project_cognito_client_scopes</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_cognito_client_scopes</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 011_insert_subscription_events.sql

### SQLの概要

利用申請承認の処理結果として、subscriptionイベントを追加する。

### 利用するテーブル

- `subscription_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>subscription_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>subscription_events</code> | <code>subscription_id</code> | <code>UUID</code> | no |
| <code>subscription_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>subscription_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>subscription_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 012_insert_audit_events.sql

### SQLの概要

利用申請承認の処理結果として、監査イベントを追加する。

### 利用するテーブル

- `audit_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>audit_events</code> | <code>audit_event_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>access_request_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>source_ip</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>user_agent</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>details</code> | <code>dict[str, Any]</code> | no |
| <code>audit_events</code> | <code>now</code> | <code>datetime</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 013_insert_idempotency_records.sql

### SQLの概要

利用申請承認の処理結果として、冪等性レコードを追加する。

### 利用するテーブル

- `idempotency_records`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>idempotency_records</code> | <code>idempotency_record_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>request_hash</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>idempotency_records</code> | <code>expires_at</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>now</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 014_insert_provisioning_steps.sql

### SQLの概要

利用申請承認の処理結果として、provisioning stepを追加する。

### 利用するテーブル

- `provisioning_steps`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_steps</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>step_order</code> | <code>int</code> | no |
| <code>provisioning_steps</code> | <code>step_name</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_service</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_action</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>error_code</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>error_message</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>started_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>finished_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 015_insert_usage_plan_stage_events.sql

### SQLの概要

利用申請承認の処理結果として、Usage Plan stageイベントを追加する。

### 利用するテーブル

- `usage_plan_stage_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>usage_plan_stage_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>usage_plan_stage_events</code> | <code>usage_plan_api_stage_id</code> | <code>UUID</code> | no |
| <code>usage_plan_stage_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>usage_plan_stage_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>usage_plan_stage_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 016_insert_client_scope_events.sql

### SQLの概要

利用申請承認の処理結果として、client scopeイベントを追加する。

### 利用するテーブル

- `client_scope_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>client_scope_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>client_scope_events</code> | <code>project_cognito_client_scope_id</code> | <code>UUID</code> | no |
| <code>client_scope_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>client_scope_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>client_scope_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 017_insert_provisioning_operation_events.sql

### SQLの概要

利用申請承認の処理結果として、provisioning operation eventsを追加する。

### 利用するテーブル

- `provisioning_operation_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operation_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operation_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 018_insert_provisioning_step_events.sql

### SQLの概要

利用申請承認の処理結果として、provisioning step eventsを追加する。

### 利用するテーブル

- `provisioning_step_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_step_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_step_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_
