<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# reject_api_access_request query

## 001_select_api_access_requests.sql

### SQLの概要

却下対象の利用申請と現在状態を確認するため、利用申請を取得する。

### 利用するテーブル

- `api_access_requests`
- `apis`
- `api_access_reviews`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `access_request_id` | `UUID` | no |

### 戻り値

| 項目 | 型 | nullable |
| --- | --- | --- |
| `access_request_id` | `UUID` | no |
| `project_id` | `UUID` | no |
| `api_id` | `UUID` | no |
| `api_stage_id` | `UUID` | no |
| `requested_auth_mode` | `str` | no |
| `requested_reason` | `str` | no |
| `requested_by` | `str` | no |
| `requested_at` | `datetime` | no |
| `api_code` | `str` | no |
| `api_name` | `str` | no |

### 条件

- `JOIN ON a.api_id = ar.api_id`
- `WHERE ar.access_request_id = @access_request_id AND NOT EXISTS(SELECT 1 FROM api_access_reviews AS rv WHERE rv.access_request_id = ar.access_request_id)`


## 002_select_api_reviewers.sql

### SQLの概要

却下者が対象APIのreviewerか確認するため、API reviewerを取得する。

### 利用するテーブル

- `api_reviewers`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `api_id` | `UUID` | no |
| `actor_principal_id` | `str` | no |
| `is_hub_admin` | `Any` | no |

### 戻り値

| 項目 | 型 | nullable |
| --- | --- | --- |
| `api_reviewer_id` | `UUID` | no |
| `api_id` | `UUID` | no |
| `reviewer_principal_id` | `str` | no |
| `reviewer_role` | `str` | no |

### 条件

- `WHERE api_id = @api_id AND (reviewer_principal_id = @actor_principal_id OR @is_hub_admin = TRUE)`


## 003_insert_api_access_reviews.sql

### SQLの概要

却下結果と却下コメントを保持するため、利用申請レビューを追加する。

### 利用するテーブル

- `api_access_reviews`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `access_review_id` | `UUID` | no |
| `access_request_id` | `UUID` | no |
| `actor_principal_id` | `str` | no |
| `review_comment` | `str` | no |
| `now` | `datetime` | no |

### 戻り値

_なし_

### 条件

_なし_


## 004_insert_access_request_events.sql

### SQLの概要

却下処理の開始と完了を追跡するため、利用申請イベントを追加する。

### 利用するテーブル

- `access_request_events`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `event_id` | `UUID` | no |
| `access_request_id` | `UUID` | no |
| `event_name` | `str` | no |
| `actor_principal_id` | `str` | no |
| `actor_type` | `str` | no |
| `now` | `datetime` | no |
| `reason` | `str` | no |
| `correlation_id` | `str` | no |
| `idempotency_key` | `str` | no |
| `event_payload` | `dict[str, Any]` | no |

### 戻り値

_なし_

### 条件

_なし_


## 005_insert_audit_events.sql

### SQLの概要

利用申請却下の処理結果として、監査イベントを追加する。

### 利用するテーブル

- `audit_events`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `audit_event_id` | `UUID` | no |
| `actor_principal_id` | `str` | no |
| `access_request_id` | `UUID` | no |
| `source_ip` | `str` | no |
| `user_agent` | `str` | no |
| `details` | `dict[str, Any]` | no |
| `now` | `datetime` | no |

### 戻り値

_なし_

### 条件

_なし_


## 006_insert_idempotency_records.sql

### SQLの概要

利用申請却下の処理結果として、冪等性レコードを追加する。

### 利用するテーブル

- `idempotency_records`

### 引数

| 項目 | 型 | nullable |
| --- | --- | --- |
| `idempotency_record_id` | `UUID` | no |
| `idempotency_key` | `str` | no |
| `request_hash` | `str` | no |
| `response_payload` | `dict[str, Any]` | no |
| `expires_at` | `datetime` | no |
| `now` | `datetime` | no |
| `actor_principal_id` | `str` | no |

### 戻り値

_なし_

### 条件

_なし_
