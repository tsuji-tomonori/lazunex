<!-- This file is generated by uv run python -m tools.generate_api_unit_test_factors. Do not edit manually. -->

# list_projects unit test factors

- Operation: `listProjects`
- Endpoint: `GET /projects`

## 1. 要因ごとの要素

### F01 条件分岐

- 対象: 条件分岐 L55: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。
- AST: `not await api_functions.has_project_list_permission(caller)`

| 要素ID | 要素 | 期待観点 |
| --- | --- | --- |
| `F01-true` | 成立 | HTTP 403 error response: caller cannot list projects; log message_id: listProjects.caller_cannot_list_projects; log summary: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。 |
| `F01-false` | 不成立 | 条件不成立側または後続処理を継続する。 |

### F02 例外処理

- 対象: 例外処理 L84: Routerで捕捉した例外によりProject一覧取得が失敗した。
- AST: `ROUTER_HANDLED_EXCEPTIONS`

| 要素ID | 要素 | 期待観点 |
| --- | --- | --- |
| `F02-normal` | 発生しない | try bodyを継続し、このexcept handlerへ遷移しない。 |
| `F02-raised` | 発生する | router error response; log message_id: listProjects.router_error; log summary: Routerで捕捉した例外によりProject一覧取得が失敗した。 |

## 2. 直積したテストケース一覧

| Case ID | F01 | F02 |
| --- | --- | --- |
| `TC001` | `成立` | - |
| `TC002` | `不成立` | `発生しない` |
| `TC003` | `不成立` | `発生する` |

## 3. テスト詳細

### TC001

| 要因 | 要素 | 期待観点 |
| --- | --- | --- |
| `F01` 条件分岐 L55: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。 | 成立 | HTTP 403 error response: caller cannot list projects; log message_id: listProjects.caller_cannot_list_projects; log summary: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。 |

### TC002

| 要因 | 要素 | 期待観点 |
| --- | --- | --- |
| `F01` 条件分岐 L55: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。 | 不成立 | 条件不成立側または後続処理を継続する。 |
| `F02` 例外処理 L84: Routerで捕捉した例外によりProject一覧取得が失敗した。 | 発生しない | try bodyを継続し、このexcept handlerへ遷移しない。 |
| API正常応答 | 正常 | HTTP 200 success response |

### TC003

| 要因 | 要素 | 期待観点 |
| --- | --- | --- |
| `F01` 条件分岐 L55: 呼び出し元がProject一覧を参照できないため、リクエストを拒否した。 | 不成立 | 条件不成立側または後続処理を継続する。 |
| `F02` 例外処理 L84: Routerで捕捉した例外によりProject一覧取得が失敗した。 | 発生する | router error response; log message_id: listProjects.router_error; log summary: Routerで捕捉した例外によりProject一覧取得が失敗した。 |
