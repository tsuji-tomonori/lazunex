<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# create_project query

## 001_select_projects.sql

### SQLの概要

Project codeの重複作成を防ぐため、既存Projectを取得する。

### 利用するテーブル

- `projects`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>projects</code> | <code>project_code</code> | <code>str</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>projects</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>projects</code> | <code>project_code</code> | <code>str</code> | no |
| <code>projects</code> | <code>name</code> | <code>str</code> | no |
| <code>projects</code> | <code>owner_principal_id</code> | <code>str</code> | no |

### 条件

- `WHERE project_code = @project_code`


## 002_insert_projects.sql

### SQLの概要

新規Projectの基本情報を保持するため、Projectを追加する。

### 利用するテーブル

- `projects`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>projects</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>projects</code> | <code>project_code</code> | <code>str</code> | no |
| <code>projects</code> | <code>name</code> | <code>str</code> | no |
| <code>projects</code> | <code>description</code> | <code>str</code> | no |
| <code>projects</code> | <code>owner_principal_id</code> | <code>str</code> | no |
| <code>projects</code> | <code>department_code</code> | <code>str</code> | no |
| <code>projects</code> | <code>now</code> | <code>datetime</code> | no |
| <code>projects</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 003_insert_project_events.sql

### SQLの概要

Project作成を履歴化するため、Projectイベントを追加する。

### 利用するテーブル

- `project_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>project_events</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>project_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>project_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>project_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>project_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>project_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 004_insert_provisioning_operations.sql

### SQLの概要

Project作成の処理結果として、provisioning operationを追加する。

### 利用するテーブル

- `provisioning_operations`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operations</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operations</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_operations</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operations</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 005_insert_project_api_keys.sql

### SQLの概要

Projectに払い出したAPI key metadataを保持するため、Project API keyを追加する。

### 利用するテーブル

- `project_api_keys`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_api_keys</code> | <code>project_api_key_id</code> | <code>UUID</code> | no |
| <code>project_api_keys</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_api_keys</code> | <code>aws_account_id</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>aws_region</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>apigw_api_key_id</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>apigw_api_key_name</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>api_key_value_hash</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>api_key_hash_key_version</code> | <code>int</code> | no |
| <code>project_api_keys</code> | <code>api_key_last4</code> | <code>str</code> | no |
| <code>project_api_keys</code> | <code>observed_enabled</code> | <code>bool</code> | no |
| <code>project_api_keys</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_api_keys</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 006_insert_project_usage_plans.sql

### SQLの概要

Project用Usage Plan metadataを保持するため、Project Usage Planを追加する。

### 利用するテーブル

- `project_usage_plans`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_usage_plans</code> | <code>project_usage_plan_id</code> | <code>UUID</code> | no |
| <code>project_usage_plans</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_usage_plans</code> | <code>aws_account_id</code> | <code>str</code> | no |
| <code>project_usage_plans</code> | <code>aws_region</code> | <code>str</code> | no |
| <code>project_usage_plans</code> | <code>apigw_usage_plan_id</code> | <code>str</code> | no |
| <code>project_usage_plans</code> | <code>usage_plan_name</code> | <code>str</code> | no |
| <code>project_usage_plans</code> | <code>default_rate_limit</code> | <code>int</code> | no |
| <code>project_usage_plans</code> | <code>default_burst_limit</code> | <code>int</code> | no |
| <code>project_usage_plans</code> | <code>default_quota_limit</code> | <code>int</code> | no |
| <code>project_usage_plans</code> | <code>default_quota_period</code> | <code>str</code> | no |
| <code>project_usage_plans</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_usage_plans</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 007_insert_project_usage_plan_keys.sql

### SQLの概要

ProjectのAPI keyとUsage Planの紐づきを保持するため、Project Usage Plan keyを追加する。

### 利用するテーブル

- `project_usage_plan_keys`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_usage_plan_keys</code> | <code>project_usage_plan_key_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_keys</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_keys</code> | <code>project_usage_plan_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_keys</code> | <code>project_api_key_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_keys</code> | <code>apigw_usage_plan_key_id</code> | <code>str</code> | no |
| <code>project_usage_plan_keys</code> | <code>apigw_usage_plan_id</code> | <code>str</code> | no |
| <code>project_usage_plan_keys</code> | <code>apigw_api_key_id</code> | <code>str</code> | no |
| <code>project_usage_plan_keys</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_usage_plan_keys</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 008_insert_project_cognito_clients.sql

### SQLの概要

Project用Cognito app client metadataを保持するため、Project Cognito clientを追加する。

### 利用するテーブル

- `project_cognito_clients`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_cognito_clients</code> | <code>project_cognito_client_id</code> | <code>UUID</code> | no |
| <code>project_cognito_clients</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_cognito_clients</code> | <code>client_type</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>cognito_user_pool_id</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>app_client_id</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>app_client_name</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>generate_secret</code> | <code>bool</code> | no |
| <code>project_cognito_clients</code> | <code>client_secret_value_hash</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>client_secret_hash_key_version</code> | <code>int</code> | no |
| <code>project_cognito_clients</code> | <code>client_secret_last4</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>allowed_oauth_flows</code> | <code>dict[str, Any]</code> | no |
| <code>project_cognito_clients</code> | <code>base_allowed_scopes</code> | <code>dict[str, Any]</code> | no |
| <code>project_cognito_clients</code> | <code>access_token_validity</code> | <code>int</code> | no |
| <code>project_cognito_clients</code> | <code>access_token_unit</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>id_token_validity</code> | <code>int</code> | no |
| <code>project_cognito_clients</code> | <code>id_token_unit</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>refresh_token_validity</code> | <code>int</code> | no |
| <code>project_cognito_clients</code> | <code>refresh_token_unit</code> | <code>str</code> | no |
| <code>project_cognito_clients</code> | <code>refresh_token_rotation_enabled</code> | <code>bool</code> | no |
| <code>project_cognito_clients</code> | <code>retry_grace_period_seconds</code> | <code>int</code> | no |
| <code>project_cognito_clients</code> | <code>enable_token_revocation</code> | <code>bool</code> | no |
| <code>project_cognito_clients</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_cognito_clients</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 009_insert_project_cognito_client_urls.sql

### SQLの概要

public clientのcallback/logout URLを保持するため、Project Cognito client URLを追加する。

### 利用するテーブル

- `project_cognito_client_urls`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_cognito_client_urls</code> | <code>client_url_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_urls</code> | <code>project_cognito_client_id</code> | <code>UUID</code> | no |
| <code>project_cognito_client_urls</code> | <code>url_type</code> | <code>str</code> | no |
| <code>project_cognito_client_urls</code> | <code>url</code> | <code>str</code> | no |
| <code>project_cognito_client_urls</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_cognito_client_urls</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 010_insert_project_members.sql

### SQLの概要

Project owner/memberを管理するため、Project memberを追加する。

### 利用するテーブル

- `project_members`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_members</code> | <code>project_member_id</code> | <code>UUID</code> | no |
| <code>project_members</code> | <code>project_id</code> | <code>UUID</code> | no |
| <code>project_members</code> | <code>member_principal_id</code> | <code>str</code> | no |
| <code>project_members</code> | <code>member_role</code> | <code>str</code> | no |
| <code>project_members</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_members</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 011_insert_idempotency_records.sql

### SQLの概要

Project作成の処理結果として、冪等性レコードを追加する。

### 利用するテーブル

- `idempotency_records`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>idempotency_records</code> | <code>idempotency_record_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>request_hash</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>idempotency_records</code> | <code>expires_at</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>now</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 012_insert_provisioning_steps.sql

### SQLの概要

Project作成の処理結果として、provisioning stepを追加する。

### 利用するテーブル

- `provisioning_steps`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_steps</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>step_order</code> | <code>int</code> | no |
| <code>provisioning_steps</code> | <code>step_name</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_service</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_action</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>error_code</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>error_message</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>started_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>finished_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 013_insert_project_member_events.sql

### SQLの概要

Project作成の処理結果として、Project memberイベントを追加する。

### 利用するテーブル

- `project_member_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_member_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>project_member_events</code> | <code>project_member_id</code> | <code>UUID</code> | no |
| <code>project_member_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_member_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>project_member_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 014_insert_project_api_key_events.sql

### SQLの概要

Project作成の処理結果として、Project API keyイベントを追加する。

### 利用するテーブル

- `project_api_key_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_api_key_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>project_api_key_events</code> | <code>project_api_key_id</code> | <code>UUID</code> | no |
| <code>project_api_key_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_api_key_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>project_api_key_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 015_insert_project_usage_plan_events.sql

### SQLの概要

Project作成の処理結果として、Project Usage Planイベントを追加する。

### 利用するテーブル

- `project_usage_plan_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_usage_plan_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_events</code> | <code>project_usage_plan_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_usage_plan_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>project_usage_plan_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 016_insert_project_usage_plan_key_events.sql

### SQLの概要

Project作成の処理結果として、Project Usage Plan keyイベントを追加する。

### 利用するテーブル

- `project_usage_plan_key_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>project_usage_plan_key_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_key_events</code> | <code>project_usage_plan_key_id</code> | <code>UUID</code> | no |
| <code>project_usage_plan_key_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>project_usage_plan_key_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>project_usage_plan_key_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 017_insert_provisioning_operation_events.sql

### SQLの概要

Project作成の処理結果として、provisioning operation eventsを追加する。

### 利用するテーブル

- `provisioning_operation_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operation_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operation_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 018_insert_provisioning_step_events.sql

### SQLの概要

Project作成の処理結果として、provisioning step eventsを追加する。

### 利用するテーブル

- `provisioning_step_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_step_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_step_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_
