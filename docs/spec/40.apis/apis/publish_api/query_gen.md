<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# publish_api query

## 001_select_apis.sql

### SQLの概要

API codeの重複登録を防ぐため、既存APIを取得する。

### 利用するテーブル

- `apis`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>apis</code> | <code>api_code</code> | <code>str</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>apis</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>apis</code> | <code>api_code</code> | <code>str</code> | no |
| <code>apis</code> | <code>name</code> | <code>str</code> | no |
| <code>apis</code> | <code>default_api_stage_id</code> | <code>UUID &#124; None</code> | yes |

### 条件

- `WHERE api_code = @api_code`


## 002_select_api_cognito_scopes.sql

### SQLの概要

custom scopeの重複登録を防ぐため、既存API Cognito scopeを取得する。

### 利用するテーブル

- `api_cognito_scopes`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_cognito_scopes</code> | <code>scope_full_name</code> | <code>str</code> | no |

### 戻り値

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_cognito_scopes</code> | <code>api_scope_id</code> | <code>UUID</code> | no |
| <code>api_cognito_scopes</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_cognito_scopes</code> | <code>cognito_user_pool_id</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>resource_server_identifier</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_name</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_full_name</code> | <code>str</code> | no |

### 条件

- `WHERE scope_full_name = @scope_full_name`


## 003_insert_provisioning_operations.sql

### SQLの概要

API公開登録の処理結果として、provisioning operationを追加する。

### 利用するテーブル

- `provisioning_operations`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operations</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operations</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>provisioning_operations</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_operations</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operations</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 004_insert_apis.sql

### SQLの概要

公開対象APIのcatalog metadataを保持するため、API catalogを追加する。

### 利用するテーブル

- `apis`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>apis</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>apis</code> | <code>api_code</code> | <code>str</code> | no |
| <code>apis</code> | <code>name</code> | <code>str</code> | no |
| <code>apis</code> | <code>description</code> | <code>str</code> | no |
| <code>apis</code> | <code>provider_name</code> | <code>str</code> | no |
| <code>apis</code> | <code>provider_contact</code> | <code>str</code> | no |
| <code>apis</code> | <code>owner_principal_id</code> | <code>str</code> | no |
| <code>apis</code> | <code>visibility</code> | <code>str</code> | no |
| <code>apis</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>apis</code> | <code>now</code> | <code>datetime</code> | no |
| <code>apis</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 005_insert_api_gateway_stages.sql

### SQLの概要

公開対象のAPI Gateway stageをLazunex上で参照するため、API Gateway stageを追加する。

### 利用するテーブル

- `api_gateway_stages`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_gateway_stages</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>api_gateway_stages</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_gateway_stages</code> | <code>aws_account_id</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>aws_region</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>apigw_rest_api_id</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>apigw_stage_name</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>invoke_url</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>custom_domain_url</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>deployment_id</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>authorizer_id</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>api_key_required_observed</code> | <code>bool</code> | no |
| <code>api_gateway_stages</code> | <code>scope_config_observed</code> | <code>str</code> | no |
| <code>api_gateway_stages</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_gateway_stages</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 006_insert_api_cognito_scopes.sql

### SQLの概要

API実行認可に使うcustom scopeを管理するため、API Cognito scopeを追加する。

### 利用するテーブル

- `api_cognito_scopes`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_cognito_scopes</code> | <code>api_scope_id</code> | <code>UUID</code> | no |
| <code>api_cognito_scopes</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_cognito_scopes</code> | <code>cognito_user_pool_id</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>resource_server_identifier</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_name</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_full_name</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>scope_description</code> | <code>str</code> | no |
| <code>api_cognito_scopes</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_cognito_scopes</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 007_insert_api_documents.sql

### SQLの概要

公開APIのOpenAPI documentを保持するため、API documentを追加する。

### 利用するテーブル

- `api_documents`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_documents</code> | <code>api_document_id</code> | <code>UUID</code> | no |
| <code>api_documents</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_documents</code> | <code>document_type</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>version_label</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>s3_uri</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>sha256</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>source_filename</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_documents</code> | <code>now</code> | <code>datetime</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 008_insert_api_reviewers.sql

### SQLの概要

利用申請の審査担当を管理するため、API reviewerを追加する。

### 利用するテーブル

- `api_reviewers`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_reviewers</code> | <code>api_reviewer_id</code> | <code>UUID</code> | no |
| <code>api_reviewers</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_reviewers</code> | <code>reviewer_principal_id</code> | <code>str</code> | no |
| <code>api_reviewers</code> | <code>reviewer_role</code> | <code>str</code> | no |
| <code>api_reviewers</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_reviewers</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 009_insert_api_events.sql

### SQLの概要

API公開登録の処理結果として、APIイベントを追加する。

### 利用するテーブル

- `api_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>api_events</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>api_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>api_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>api_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>api_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>api_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>api_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 010_insert_audit_events.sql

### SQLの概要

API公開登録の処理結果として、監査イベントを追加する。

### 利用するテーブル

- `audit_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>audit_events</code> | <code>audit_event_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>api_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>audit_events</code> | <code>source_ip</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>user_agent</code> | <code>str</code> | no |
| <code>audit_events</code> | <code>details</code> | <code>dict[str, Any]</code> | no |
| <code>audit_events</code> | <code>now</code> | <code>datetime</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 011_insert_idempotency_records.sql

### SQLの概要

API公開登録の処理結果として、冪等性レコードを追加する。

### 利用するテーブル

- `idempotency_records`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>idempotency_records</code> | <code>idempotency_record_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>request_hash</code> | <code>str</code> | no |
| <code>idempotency_records</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>idempotency_records</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>idempotency_records</code> | <code>expires_at</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>now</code> | <code>datetime</code> | no |
| <code>idempotency_records</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 012_insert_provisioning_steps.sql

### SQLの概要

API公開登録の処理結果として、provisioning stepを追加する。

### 利用するテーブル

- `provisioning_steps`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_steps</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_steps</code> | <code>step_order</code> | <code>int</code> | no |
| <code>provisioning_steps</code> | <code>step_name</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_service</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>aws_action</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>request_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>response_payload</code> | <code>dict[str, Any]</code> | no |
| <code>provisioning_steps</code> | <code>error_code</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>error_message</code> | <code>str</code> | no |
| <code>provisioning_steps</code> | <code>started_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>finished_at</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_steps</code> | <code>actor_principal_id</code> | <code>str</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 013_insert_api_stage_events.sql

### SQLの概要

API公開登録の処理結果として、API stageイベントを追加する。

### 利用するテーブル

- `api_stage_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_stage_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>api_stage_events</code> | <code>api_stage_id</code> | <code>UUID</code> | no |
| <code>api_stage_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_stage_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>api_stage_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 014_insert_api_scope_events.sql

### SQLの概要

API公開登録の処理結果として、API scopeイベントを追加する。

### 利用するテーブル

- `api_scope_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_scope_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>api_scope_events</code> | <code>api_scope_id</code> | <code>UUID</code> | no |
| <code>api_scope_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_scope_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>api_scope_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 015_insert_api_reviewer_events.sql

### SQLの概要

API公開登録の処理結果として、API reviewerイベントを追加する。

### 利用するテーブル

- `api_reviewer_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>api_reviewer_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>api_reviewer_events</code> | <code>api_reviewer_id</code> | <code>UUID</code> | no |
| <code>api_reviewer_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>api_reviewer_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>api_reviewer_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 016_insert_provisioning_operation_events.sql

### SQLの概要

API公開登録の処理結果として、provisioning operation eventsを追加する。

### 利用するテーブル

- `provisioning_operation_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_operation_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>operation_id</code> | <code>UUID</code> | no |
| <code>provisioning_operation_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_operation_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_operation_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_


## 017_insert_provisioning_step_events.sql

### SQLの概要

API公開登録の処理結果として、provisioning step eventsを追加する。

### 利用するテーブル

- `provisioning_step_events`

### 引数

| 取得元テーブル | 項目 | 型 | nullable |
| --- | --- | --- | --- |
| <code>provisioning_step_events</code> | <code>event_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>operation_step_id</code> | <code>UUID</code> | no |
| <code>provisioning_step_events</code> | <code>event_name</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_principal_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>actor_type</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>now</code> | <code>datetime</code> | no |
| <code>provisioning_step_events</code> | <code>reason</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>correlation_id</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>idempotency_key</code> | <code>str</code> | no |
| <code>provisioning_step_events</code> | <code>event_payload</code> | <code>dict[str, Any]</code> | no |

### 戻り値

_なし_

### 条件

_なし_
